defaults:
  - model: resnet50
  - dataset: cifar10
  - _self_

training:
  batch_size: 32
  lr: 0.001
  epochs: 10
  device: auto
  seed: 42

logging:
  log_interval: 10
  save_checkpoint: true
  tensorboard: true

project:
  name: utmist-ml-project
