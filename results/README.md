# results/

Curated outputs worth keeping across runs — plots, metric summaries, write-ups. Unlike `outputs/` (every Hydra run), this is for final or notable results you want to reference later.

This directory is gitignored by default. To commit a specific file: `git add -f results/my_plot.png`.
